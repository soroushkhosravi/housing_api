apiVersion: v1
kind: Service
metadata:
  name: {{ include "housing-api.fullname" . }}
  labels:
    {{- include "housing-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "housing-api.selectorLabels" . | nindent 4 }}
